import { Children, ReactNode } from "react"

type Props = {
    url: string;
    children: ReactNode;
}

export const Foto = ({url}: Props)=>{
    return(
        <>
               
          
           <p>pegpoi</p>
          
           
        </>
    )
}